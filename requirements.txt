# Generic training/export stack ONLY — no game packages here. Games install
# separately (`task setup GAME=<slug>` runs `pip install lockstep-game-<slug>`)
# and are discovered at runtime through the `lockstep.training_games` entry
# point; each game wheel pulls lockstep-train (the platform's game-agnostic
# wasm-stepping host) and carries its own codec + prebuilt agent shells.
gymnasium>=1.0
numpy>=1.26
torch>=2.4
onnx>=1.16
onnxruntime>=1.18
# torch.onnx.export's exporter imports this at export time.
onnxscript>=0.1
