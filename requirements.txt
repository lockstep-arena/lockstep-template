# Generic training/export stack ONLY — there are no per-environment
# packages, full stop. `task setup` additionally installs `lockstep-train`
# (the platform's environment-agnostic wasm-stepping host); everything
# per-environment comes from the CDN release and the engine's own
# declaration at runtime.
gymnasium>=1.0
# The multi-agent API for `task train PARALLEL=1` (both seats learning at
# once, on any multi-seat environment).
pettingzoo>=1.24
numpy>=1.26
# <2.14: torch 2.14's exporter emits `noop_with_empty_axes` on ReduceMean even at
# opset 17, which onnxruntime refuses to load ("Unrecognized attribute"). Lift the
# cap once the export path moves to opset 18 end to end.
torch>=2.4,<2.14
onnx>=1.16
onnxruntime>=1.18
# torch.onnx.export's exporter imports this at export time.
onnxscript>=0.1
