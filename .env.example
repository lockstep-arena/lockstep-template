# Needed for `task upload`, and for every task on an assessment-only
# environment (its engine downloads only with your key). Create a key on the
# Authorization page at https://lockstep.it — a plain user key, no special
# role. `task doctor` reports whether you need it and whether it is accepted.
LOCKSTEP_API_KEY=
